# AI Buyer Persona Profile Generation · Doc ROI

English deployment shell for the Doc ROI Buyer Persona application.

## Vercel

Use the default Vite settings:

- Framework preset: Vite
- Root directory: `./`
- Build command: `npm run build`
- Output directory: `dist`

The app loads the English version of the Buyer Persona tool with `?lang=en`.
